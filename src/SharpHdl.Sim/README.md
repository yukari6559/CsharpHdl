# SharpHdl.Sim

Cycle-style simulation for [SharpHdl.Core](https://www.nuget.org/packages/SharpHdl.Core) modules (`Run` / `Set` / `Get` / `Settle` / `Advance`).

**Current scope (S1):** single-module Comb + Seq. Memory and hierarchy come in a later release (S2). Assert with ordinary xUnit.

## Install

```xml
<PackageReference Include="SharpHdl.Core" Version="0.3.1" />
<PackageReference Include="SharpHdl.Sim" Version="0.3.1" />
```

Optional with emit:

```xml
<PackageReference Include="SharpHdl.Emit" Version="0.3.1" />
```

## Docs

- Repository: https://github.com/yukari6559/CsharpHdl
- Consumer guide: https://github.com/yukari6559/CsharpHdl/blob/main/docs/consumers.md

## License

MIT
